version: 2

build:
  os: ubuntu-22.04
  tools:
    python: "3.11"

python:
  install:
    - requirements: docs/requirements.txt

sphinx:
  configuration: docs/conf.py

# PDF temporarily disabled - pdflatex doesn't support Cyrillic
# formats:
#   - pdf
